"""
Core application for Watchtower (Vagt).

This app provides shared utilities, base models, mixins, and common functionality
used across other apps in the project.

Contents:
- Base model classes with common fields (created_at, updated_at, etc.)
- Utility functions and helpers
- Custom template tags and filters
- Shared validators and form widgets
"""

default_app_config = "apps.core.apps.CoreConfig"
